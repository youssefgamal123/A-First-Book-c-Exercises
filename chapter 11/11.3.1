1)
A.

From Class to built in data type conversion --> can be done by using operator conversion funtions
From built in data types to class conversion --> can be done by using conversion constructor
From class to class --> can be done by using operator conversion functions and changing the type of operation to the other class name

B)

Type conversion constructor: it's a constructor that can be used to do data type conversion from an built in data type to a class
conversion operator functions : they are type of operator functions except that the name of the operation after operator function is the data type we want to change to ,It's used to do data type conversion between a class and a built in data type ;
