A)

Date Date::operator-(long int days){
Date temp;
temp.day = day-days;
temp.month = month;
temp.year= year;

while(temp.day<1){
temp.month--;
temp.day+=30;
}

while (temp.month  < 1){
temp.year--;
temp.month+=12
}

return temp;

}


B)
Date Date::operator-(Date& date2){
Date temp; // temporary object to store the yiedl data because we don't want to alter the data of the other object
temp.days = day-date2.day;
temp.month = month - dat2.month;
temp.year = year - date2.year;


return temp;
\
}
